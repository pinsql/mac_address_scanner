# mac_address_scanner
#first run this on your terminal and install the nessesary dipendancies 
brew install arp-scan

#Then make it executable.
chmod +x mac_scanner.py

#Then run it.
sudo ./mac_scanner.py
